//
//  Color+Ext.swift
//  DubDubGrub
//
//  Created by Simon Berner on 20.12.21.
//

import SwiftUI

extension Color {
    static let brandPrimary = Color("brandPrimary") // our app brands (design) primary color
    static let brandSecondary = Color(.systemPink) // our app brands (design) secondary color
    static let grubRed = Color(.systemPink) // the stuff we actually want to be red in the app
}
